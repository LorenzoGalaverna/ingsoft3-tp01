# Copiá este archivo a .env (git lo ignora) y ajustá si hace falta.
# Backend del walking skeleton — conexión al PostgreSQL local (dev).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/habits"
PORT=8080
